To access the kubeconfig for your cluster via the VCN-Native public endpoint, copy the following command:
oci ce cluster create-kubeconfig --cluster-id [cluster OCID] --file $HOME/.kube/config --region sa-saopaulo-1 --token-version 2.0.0  --kube-endpoint PUBLIC_ENDPOINT


To access the kubeconfig for your cluster via the VCN-Native private endpoint, copy the following command:
oci ce cluster create-kubeconfig --cluster-id [cluster OCID] --file $HOME/.kube/config --region sa-saopaulo-1 --token-version 2.0.0  --kube-endpoint PRIVATE_ENDPOINT


original structure:
ssh (folder)
.terraform (folder)
resources.tf
compartments.tf
main.tf
terraform.tfvars
variables.tf
.terraform.lock.hcl
output.tf
terraform.tfstate
readme
terraform.tfstate.backup
.gitignore